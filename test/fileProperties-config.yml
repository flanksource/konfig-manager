hierarchy:
  - name: sample
    namespace: default
    kind: ConfigMap
    type: properties
    key: application.properties
  - name: "spring-defaults-{{.name}}"
    namespace: default
    kind: ConfigMap
